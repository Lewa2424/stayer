package com.example.stayer.pathnet.model

/**
 * Режим редактирования на экране карты.
 * Active editor mode on the full-screen map.
 */
enum class PathEditorMode {
    EXTEND,
    ADD_BRANCH,
    BEND,
    DELETE,
}
