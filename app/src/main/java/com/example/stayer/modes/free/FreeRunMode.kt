package com.example.stayer.modes.free

const val FREE_RUN_MODE = 3
const val FREE_RUN_HISTORY_MODE = "free"

/**
 * Возвращает человекочитаемое название режима свободного бега.
 * Returns the human-readable label for the free run mode.
 */
fun freeRunLabel(): String = "Свободный бег"
